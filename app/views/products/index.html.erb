<table>
  <tr>
    <th>Title</th>

    <th>Description</th>
  </tr>

  <% @products.each do |product| %>
    <tr>
      <td><%= product.title %></td>

      <td><%= product.description %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'New Product', new_product_path %>
