<li><div>
  <%= link_to(image_tag(product.photo.url), product_path(product)) %>

  <div>
    <h3><%= link_to(product.title, product_path(product)) %></h3>
  </div>

  <div>
    <%= button_to(t('.add_to_basket'), line_items_path(product_id: product)) %>
  </div>
</div></li>
