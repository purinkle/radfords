<li><div>
  <%= link_to(image_tag(product.photo.url(:thumbnail)), product_path(product)) %>

  <div>
    <h3><%= link_to(product.title, product_path(product)) %></h3>
  </div>

  <%= product_button(product) %>
</div></li>
